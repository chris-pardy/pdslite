{
  "name": "@pdslite/pds",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsdown": "^0.12.0",
    "vitest": "^3.1.0",
    "typescript": "^5.8.0"
  }
}
