{
  "name": "@pdslite/pds",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "test": "vitest run",
    "lint": "oxlint src/"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@hono/node-server": "^1.14.0"
  },
  "devDependencies": {
    "@pdslite/plugin-allow-all": "workspace:*",
    "@pdslite/plugin-auth-password": "workspace:*",
    "tsdown": "^0.12.0",
    "vitest": "^3.1.0",
    "typescript": "^5.8.0"
  }
}
